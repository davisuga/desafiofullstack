const boxStyles = {
  baseStyle: {},
  sizes: {
    xl: {
      h: "56px",
      fontSize: "lg",
      px: "32px",
    },
  },
  variants: {},
};
export type variants = typeof boxStyles.variants;

export default boxStyles;
